query {
    viewer {
        organization {
            projects {
                edges {
                    node {
                        id
                        name
                        api
                    }
                }
            }
        }
    }
}